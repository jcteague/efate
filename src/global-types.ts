import {FieldBuilder} from "./types";
import {withValueBuilder} from "./property-builders/string-field-builder";

// interface String {
//   withValue: (valuePrefix: string) => FieldBuilder;
// }

// Object.defineProperty(String.prototype, 'withValue', {value: withValueBuilder} );