
import GraphqlFixture from './graphql-fixture';

describe('graphql-schema-tests', () => {
  it('should generate a fixture for a query', function () {
      const graphqlFixture = new GraphqlFixture({schemaSrcPath: `${__dirname}/../fixtures/schema.graphql`})


  })
})